# Root-level convenience copy. The backend reads backend/.env (see backend/.env.example).
# This file documents the variables used by the local enrichment backend.

PORT=8787
DEEPSEEK_API_KEY=
BRAVE_SEARCH_API_KEY=
DATABASE_PATH=./data/enrichment.sqlite
ALLOWED_EXTENSION_IDS=
DATA_RETENTION_DAYS=30
